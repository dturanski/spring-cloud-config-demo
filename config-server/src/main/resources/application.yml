spring:
   config:
       name: configserver
   cloud:
       config:
           server:
               git:
                  uri: https://github.com/dturanski/config-repo
#               overrides:
#                  info.foo: bar
server:
   port: 8888
